# `services`

This module will stay only for the logical part and database operations. we will be creating reusable functions and code logic in services which we later call from the controllers to fetch relevent data.

## Usage

```
const services = require('services');

```

Note: Make sure whenever you create new service file, you will need to register it inside `packages/services/src/index.js`.
