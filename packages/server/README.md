# `server`

This is a heart of our entire code structure and base of root configuration. our command will pass the signal to server module and it will start the app in either `watch` mode or `production` mode based on command we executed.
